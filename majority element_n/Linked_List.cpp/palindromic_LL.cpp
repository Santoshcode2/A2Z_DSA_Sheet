#include<bits/stdc++.h>
using namespace std;

class Node{
    public:
    Node* next;
};
class Solution{
 public:
 bool isPalindrome(Node* head){
  if(head==NULL || head->next==NULL) return true;
 }
};

